# Colab 노트북 설정 가이드

## 문제 상황

"요청한 파일이 없습니다" 오류가 발생하는 이유:
- Colab 노트북 URL이 아직 설정되지 않음
- `lib/config.ts`에서 `YOUR_NOTEBOOK_ID`가 실제 노트북 ID로 변경되지 않음

## 해결 방법

### 1단계: Colab 노트북 생성

#### 1. Google Colab 접속
1. https://colab.research.google.com 접속
2. Google 계정으로 로그인
3. "새 노트북" 클릭

#### 2. 노트북 제목 설정
```
브릿지 모의고사 - 1회차
```

#### 3. 노트북 내용 작성

```python
# ============================================
# 브릿지 모의고사 - 1회차
# AICE 자격증 1회차 모의고사
# ============================================

# 필요한 라이브러리 import
import pandas as pd
import numpy as np

print("=" * 50)
print("브릿지 모의고사 - 1회차")
print("=" * 50)
print()

# ============================================
# 모의고사 데이터 로드
# ============================================
print("📊 모의고사 데이터 로드 중...")

# CSV 파일 URL (웹사이트에서 복사한 URL 사용)
csv_url = 'https://bridge-mock-exam-nextjs.vercel.app/data/exam1.csv'

# CSV 파일 읽기
df = pd.read_csv(csv_url, encoding='utf-8')

print(f"✅ 데이터 로드 완료! (총 {len(df)}문제)")
print()
print("=" * 50)
print("문제 미리보기")
print("=" * 50)
print(df.head())
print()

# ============================================
# 문제 풀이 시작
# ============================================
print("=" * 50)
print("문제 풀이")
print("=" * 50)
print()

# 문제를 하나씩 출력
for idx, row in df.iterrows():
    question_num = row['문제번호']
    question = row['문제']
    choice1 = row['선택지1']
    choice2 = row['선택지2']
    choice3 = row['선택지3']
    choice4 = row['선택지4']
    
    print(f"\n문제 {question_num}: {question}")
    print(f"① {choice1}")
    print(f"② {choice2}")
    print(f"③ {choice3}")
    print(f"④ {choice4}")
    print("-" * 50)
```

#### 4. 노트북 저장
- "파일" → "저장" 클릭
- Google Drive에 자동 저장됨

### 2단계: 노트북 공유 설정

#### 1. 공유 버튼 클릭
- 노트북 우측 상단 "공유" 버튼 클릭

#### 2. 공유 설정
- "링크가 있는 모든 사용자" 선택
- 권한: "뷰어" 또는 "편집자" 선택
- "완료" 클릭

#### 3. 공유 링크 복사
- 공유 링크 형식: `https://colab.research.google.com/drive/YOUR_NOTEBOOK_ID`
- `YOUR_NOTEBOOK_ID` 부분을 복사

### 3단계: 웹사이트 설정 업데이트

`lib/config.ts` 파일을 열어서 실제 Colab URL로 변경:

```typescript
export const EXAM_DATA: ExamData[] = [
  {
    id: 1,
    title: '1회차 모의고사',
    colabUrl: 'https://colab.research.google.com/drive/실제_노트북_ID', // 여기에 실제 ID 입력
    csvFilename: 'exam1.csv',
    description: 'AICE 자격증 1회차 모의고사',
  },
  // ...
];
```

### 4단계: 각 모의고사별 노트북 생성

1. **1회차 모의고사** 노트북 생성
   - CSV URL: `exam1.csv`
   - 노트북 ID 복사 → `lib/config.ts`에 추가

2. **2회차 모의고사** 노트북 생성
   - CSV URL: `exam2.csv`
   - 노트북 ID 복사 → `lib/config.ts`에 추가

3. **3회차 모의고사** 노트북 생성
   - CSV URL: `exam3.csv`
   - 노트북 ID 복사 → `lib/config.ts`에 추가

### 5단계: 변경사항 배포

```bash
cd C:\Users\COM-JY\Desktop\Task\bridge-mock-exam-nextjs
git add lib/config.ts
git commit -m "Update Colab notebook URLs"
git push origin main
```

## 빠른 테스트

Colab 노트북을 생성하기 전에 테스트하려면:

1. 임시로 Colab 노트북 하나 생성
2. 공유 링크 복사
3. `lib/config.ts`에 모든 모의고사에 같은 URL 임시로 설정
4. 배포 후 테스트
5. 나중에 각각 다른 노트북으로 교체

## 체크리스트

- [ ] Google Colab 접속
- [ ] 새 노트북 생성
- [ ] 노트북 내용 작성
- [ ] 노트북 저장
- [ ] 공유 설정 (링크가 있는 모든 사용자)
- [ ] 공유 링크에서 노트북 ID 복사
- [ ] `lib/config.ts`에 실제 URL 입력
- [ ] 변경사항 커밋 및 푸시
- [ ] 배포 확인
- [ ] 웹사이트에서 Colab 버튼 테스트

## 참고

- Colab 노트북은 Google Drive에 저장됩니다
- 공유 링크는 누구나 접근 가능하도록 설정해야 합니다
- 노트북 ID는 URL의 `/drive/` 뒤에 있는 부분입니다



